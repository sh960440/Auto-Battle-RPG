namespace Data
{
    /// <summary>
    /// Encounter feel for a stage band. Expressed through enemy stats and group size, not a type chart.
    /// </summary>
    public enum EncounterPressureType
    {
        Balanced,
        HighDefense,
        HighSpeedSwarm,
        Mixed
    }
}
